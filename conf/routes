# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Import Secure routes
*       /                                       module:secure

# Home page
GET     /                                       Application.index

# Administration
*       /admin                                  module:crud

# Ignore favicon requests
GET     /favicon.ico                            404

# Map static resources from the /app/public folder to the /public path
GET     /public/                                staticDir:public
GET 	/tabs/show/{id}							Tabs.show
GET 	/tabs/list/{artist}				 			Tabs.list
# Catch all



#Sessions

GET     /collaborate/{id}   Collaborate.index
GET     /sessions/{id}      Sessions.form
GET		/sessions/new		Sessions.form
POST	/sessions/new		Sessions.save

POST    /bands/?             Bands.create
POST    /tabs/?             Tabs.create

# Import CRUD routes
*      /admin              module:crud

*       /{controller}/{action}                  {controller}.{action}

*       /{controller}                  {controller}.index